/*var roq = require('../')
  , assert = require('assert')
  , path = require('path')
;

describe('.configure( `development`)', function(){
  
  it('.cache should return true', function(done){
    
    console.log( roq.configure( 'development', path.resolve('./test','fixtures/config')));
    
    roq.configure( 'development', path.resolve('./test','fixtures/config')).cache.should.be.true;
    done();
  })
  
  it('.cache should return false', function(done){
    
    console.log( roq.configure( 'development', path.resolve('./lib','config')));
    
    roq.configure( 'development', path.resolve('./lib','config')).cache.should.be.false;
    done();
    
  })
  
})


*/